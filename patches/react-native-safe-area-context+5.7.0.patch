diff --git a/node_modules/react-native-safe-area-context/android/build.gradle b/node_modules/react-native-safe-area-context/android/build.gradle
index f324b9a..8ea906c 100644
--- a/node_modules/react-native-safe-area-context/android/build.gradle
+++ b/node_modules/react-native-safe-area-context/android/build.gradle
@@ -32,15 +32,20 @@ def isNewArchitectureEnabled() {
 }
 
 apply plugin: 'com.android.library'
-apply plugin: 'kotlin-android'
+def agpMajor = com.android.Version.ANDROID_GRADLE_PLUGIN_VERSION.tokenize('.')[0].toInteger()
+
+// AGP 9 handles Kotlin differently via built-in Kotlin support, while AGP 8
+// still needs the classic kotlin-android plugin path for mixed Java/Kotlin source sets.
+if (agpMajor < 9) {
+    apply plugin: 'kotlin-android'
+}
 
 if (isNewArchitectureEnabled()) {
     apply plugin: "com.facebook.react"
 }
 
 android {
-    def agpVersion = com.android.Version.ANDROID_GRADLE_PLUGIN_VERSION
-    if (agpVersion.tokenize('.')[0].toInteger() >= 7) {
+    if (agpMajor >= 7) {
         namespace "com.th3rdwave.safeareacontext"
         buildFeatures {
             buildConfig true
@@ -82,17 +87,11 @@ android {
         exclude "**/libreact_render*.so"
     }
     sourceSets.main {
-        java {
-            if (isNewArchitectureEnabled()) {
-                srcDirs += [
-                    "src/fabric/java",
-                    "${project.buildDir}/generated/source/codegen/java"
-                ]
-            } else {
-                srcDirs += [
-                    "src/paper/java"
-                ]
-            }
+        if (isNewArchitectureEnabled()) {
+            java.directories.add("${project.buildDir}/generated/source/codegen/java") // contains only java files
+            kotlin.directories.add("src/fabric/java") // contains only kotlin files
+        } else {
+            java.directories.add("src/paper/java") // contains both java and kotlin files
         }
     }
 }
